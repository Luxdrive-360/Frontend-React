const API_GATEWAY_URL = 'http://localhost:8080'; // or API gateway if using one

export const USER_SERVICE = `${API_GATEWAY_URL}/users`;
// export const BOOKING_SERVICE = `${API_GATEWAY_URL}/booking-service`;
// Add more services as needed
